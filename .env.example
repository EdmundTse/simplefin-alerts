# SimpleFin Setup Token (base64 encoded, used on first run to claim access URL)
# Only needed once - after claiming, the access URL is saved to the data file
SIMPLEFIN_SETUP_TOKEN=

# Apprise notification URL (optional - omit to disable notifications)
APPRISE_URL=

# Apprise tag for routing notifications (optional)
APPRISE_TAG=
